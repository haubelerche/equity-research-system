[pytest]
# App tests live under tests/ and run on a plain Python 3.11 env.
# Restrict collection so `pytest` never wanders into non-test trees.
testpaths = tests
norecursedirs = scraper .git __pycache__ data output storage eval scale vnstock
markers =
    integration: marks tests as live integration tests (deselect with -m "not integration")
